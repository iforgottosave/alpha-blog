<nav class="navbar navbar-expand-lg navbar-dark bg-danger">
  <div class="container-fluid">
    <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand", id: "logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
        data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
        aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <% if !logged_in? %>
          <%= link_to "Join", join_path, class: "nav-link" %>
        <% end %>

        <%= link_to "Articles", articles_path, class: "nav-link" %>

        <li class="nav-item">
          <%= link_to "Bloggers", users_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "About", about_path, class: "nav-link" %>
        </li>
      </ul>

      <li class="navbar-nav mb-2 me-1 mb-lg-0 nav-item dropdown">

        <% if logged_in? %>
          <%= link_to "Create", new_article_path, class: "nav-link" %>
        <% end %>
        <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">Account </a>

        <ul class="dropdown-menu">
          <% if logged_in? %>
            <%= link_to "My profile", current_user, class: "dropdown-item" %>
            <%= link_to "Account settings", edit_user_path(current_user), class: "dropdown-item" %>
          <% else %>
            <%= link_to "Sign Up", join_path, class: "dropdown-item" %>
          <% end %>

          <li><hr class="dropdown-divider"></li>

          <% if logged_in? %>
            <%= link_to "Logout", logout_path, class: "dropdown-item" %>
          <% else %>
            <%= link_to "Login", login_path, class: "dropdown-item" %>
          <% end %>
        </ul>
      </li>

      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-light" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>